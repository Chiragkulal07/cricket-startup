import React from 'react';

const Ball = () => {
  return (
    <div className="ball">
      {/* Ball implementation */}
      <span>Ball</span>
    </div>
  );
};

// FIX: Ensure only one default export
export default Ball;
